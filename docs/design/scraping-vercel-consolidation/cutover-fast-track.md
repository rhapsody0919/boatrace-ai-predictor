# Vercel切り替えの短縮方針（独立レビュー反映版）

> **状態: 採用済み（2026-09-21、ユーザー承認）。** 独立レビューを反映した改訂版を承認し、加えて、(1)前倒し: 独立したジョブは1日に最大3件まで切り替える（G8の緩和）、(2)完了の定義Bの実測を、7日から**5日（土日を含む）**に短縮する、を承認した。ユーザーは、短縮した進め方と切り替えの権限を承認した（2026-09-21）が、「調査・文書化・独立レビューを終えてから実施する」と指示した。レビューの結果、**ドラフトの一部（オッズのlive並走の省略、夜間の切り替え、監視の前提）を修正した**（§9）。

対応: [plan.md §4.6・§4.7](./plan.md)（現行の並走方式）/ [verification-runbook.md](./verification-runbook.md) / [orchestration.md](./orchestration.md) / [ADR-0059](../../adr/0059-new-endpoint-timeout-security-monitoring-standards.md) 4

## 1. 目的

データ取得のVercel一本化の期間を短縮する。現行の設計（plan.md §4.6）は、1ジョブごとに「shadow 3日 → live並走 3〜7日 → GitHub側停止 → 7日実測」を1ジョブずつ進めるため、完了は11月中旬の見込みだった。

## 2. 現行設計からの変更点（レビュー反映後）

| 項目 | 現行（plan.md §4.6） | 短縮方針 |
|---|---|---|
| 分類の軸 | 全ジョブ共通 | **取り直せるか**で分ける（§3）。取り直せるジョブは短縮、取り直せない（オッズ）は、live並走を1〜2日残す |
| shadow | 3日 | 復旧可能なジョブは、原則1日。**オッズはshadowを省き、live並走に置き換える**（shadowも同量を取得するため、取得負荷は増えない） |
| live並走 | 3〜7日 | 復旧可能なジョブ: 行わない。オッズ: 1〜2日（`source`で区別） |
| GitHub側の停止 | 静的な変数`SKIP_<JOB>_ON_GHA` | **フェイルセーフ付き**: 変数がtrueでも、Vercelが`live`かつ直近の成功が新しくないときは、GitHub側が実行する（§6。自動フェイルオーバー） |
| 切り替えの時間帯 | 夜間（レースの無い時間帯） | 復旧可能なジョブ: **日中、親が対話中に**（§4.2の期限つきSQLを併用）。オッズ: 早朝（06:30〜07:00）に、人間のgoのもとで |
| 独立ジョブ | 1ジョブずつ | 復旧可能なジョブは、1つの観測窓（約1日）につき1件。shadowは並行してよい |
| 完了の定義Bの実測 | 切り替え後の土日を含む直近7日 | **土日を含む直近5日**（2026-09-21にユーザーが短縮。`.claude/rules/data-acquisition.md`も更新済み）。週末と平日の両方を含むことが条件 |

**変えないもの**: shadowの実施（復旧可能なジョブ）、数値基準、切り戻しの手順、完了の定義A・B・C、ADR-0067の取得先への負荷の配慮。

## 3. 「取り直せるか」による分類（レビューの指摘#3・#4を反映）

| 区分 | ジョブ | 取りこぼしの影響 | 方針 |
|---|---|---|---|
| 復旧可能 | 結果取得・result_catchup、Kファイル同期（4日ルックバック）、日次ジョブ、pcexpect、（races-initはGitHubの07:00初期化が自己修復するため、損失は約2時間の遅れ） | 後から取り直せる（GitHub側が+90分以内の未完了レースを自己修復、次の実行で回復） | shadow 1日 → live化とGitHub側停止 |
| 取り直せない | **オッズ**（時系列。取りこぼした窓は永久に失われる） | 永久損失 | shadowを省き、live並走1〜2日 → 早朝にGitHub側を停止。**人間のgoを残す** |

**live並走が追加で分かること**（ドラフトの「書き込みだけ」は誤り）: (1)`source`別の窓内取得率の直接比較（runbook M-3のSQL）、(2)書き込みの経路（`race_odds`のupsert、再試行時の既存行の読み込み`coalesceRow`）、(3)結果取得の`trg_update_predictions`連鎖（結果の書き込みで`predictions`・`bet_recommendations`が全行再UPDATEされる。WS8）、(4)live限定の処理（onTick、catch-upの書き込み、`fixMissingHitFlags`）、(5)監視のlive限定の経路。

**オッズのshadowの限界**: ダイジェストは構造（キー集合）のみで値を見ず、パーサーはGitHub側と共有しているため、パース差はほぼ検出されない。shadowは`race_odds`へ書かず、再試行の既存行読み込みも通らない。

**オッズのlive並走の負荷**: shadowも同量の取得（1日約5,000ページ）を行うので、shadowをlive並走に**置き換える**なら、取得負荷は増えない。追加は書き込み（約2MB/日）と、画面のオッズ履歴に近接行が重複して出ることだけ。

## 4. ジョブ別の切り替え基準と手順

### 4.1 共通の基準（shadow→live。復旧可能なジョブ。全て満たすこと）

| 基準 | 内容 | 確認の手段 |
|---|---|---|
| G1 一致率 | shadowのダイジェストが、既存基盤の書いた値と一致する率が99%以上。標本は、その日の全スロットの70%以上。**結果取得は約170スロット/日で、不一致0件でも失敗率の95%上限は約1.8%のため、99%は統計的に証明できない**。復旧可能であることを理由に許容する | `check-*-shadow.js` |
| G2 未実行 | `expired`が0件（うち`attempts=0`の未実行が0件） | `check-*-shadow.js`（`scrape-monitor`は、shadowの`expired`を集計しない） |
| G3 所要時間 | 1回の呼び出しの所要時間のp95が、`maxDuration`の80%以内。ソフトデッドライン中断0件 | **Vercelのランタイムログ**（DBには残らない） |
| G4 取得先 | サーキットブレーカーが、一度も開いていない。429・503は、`politeFetch`が内部で再試行するため、DBには残らない。Vercelログの目視で補う | `scrape_job_state`の`host:boatrace.jp`、Vercelログ |
| G5 遅延（窓型） | 各窓が、期限+許容幅の内側で完了し、窓内取得率が98%以上の見込み。**旧基盤以上、ではなく98%以上**（旧基盤の実測は§8） | `check-*-shadow.js` |
| G6 監視 | shadow中は、`check-*-shadow.js`で確認する（`scrape-monitor`はshadowの窓内取得率・expiredを数えない）。**切り替え前に、日次サマリー（scrape-summary、00:10 JST）が、Slackへ実際に届いたことを確認する**（`notified`が空で、これまでSlackへ送信した履歴がない） | ユーザーのSlack確認 |
| G7 時間帯 | 復旧可能なジョブ: 日中、親が対話中に切り替える。**最初の実書き込みと、次の監視評価を、親が観測できる時間帯に切り替える**（無人の夜間に切り替えない。夜間に切り替えると、最初の実書き込みは翌朝で、監視は約7時間、空白になる） | — |
| G8 観測窓 | **件数の上限は設けない**（2026-09-21夕方、ユーザーが『前倒しなので、1日3件を超えてよい。なるべく早く完了させたい』と判断。当日の午前は「1日に最大3件」だった）。ただし、(1)1件ごとに1コマンドで切り戻せること、(2)異常が出たら、直近の切り替えから順に戻して切り分ける（同時に複数を動かすほど、原因の特定が遅れる）、(3)同じデータを扱うジョブ（展示とレース情報）は、先のジョブの最初の窓が完了してから次を切り替える | 日程 |

### 4.2 手順（復旧可能なジョブ）

1. **切り替え前**: 当日の`shadow`で`done`になったスロットのうち、**期限+許容幅がまだ過ぎていないものだけ**を、`pending`に戻す。期限条件が無いと、日中に実行したとき、許容幅を過ぎたスロットが`run_mode=NULL`・`attempts>0`のまま`expired`になり、monitorが一斉に通知し、窓内取得率が崩れる:
   ```sql
   UPDATE scrape_slots s
      SET status='pending', done_at=NULL, outcome=NULL, run_mode=NULL, next_attempt_at=NULL,
          result_digest=NULL, rows_written=NULL
     FROM races r
    WHERE r.race_id = s.race_id AND s.job = '<ジョブ>' AND s.run_mode = 'shadow' AND s.status = 'done'
      AND s.race_date = (now() AT TIME ZONE 'Asia/Tokyo')::date
      AND ((r.race_date + r.start_time) AT TIME ZONE 'Asia/Tokyo')
          + make_interval(mins => s.offset_min + <graceMin>) >= now();
   ```
2. `scrape_job_state.mode`を`live`にする（DBの更新）。
3. 同じ操作で、GitHub側のリポジトリ変数`SKIP_<JOB>_ON_GHA=true`を設定する（§6のフェイルセーフにより、Vercelが不健全ならGitHub側が実行する）。
4. 最初の実書き込み（次の窓）が`live`で完了することを確認する。`scrape-monitor`のSlack通知と`check-*-shadow.js`（live用の出力）を、当日中、細かく確認する。結果取得は、`result_catchup`の初回のlive実行（00:30 JST）で、`fixMissingHitFlags`が本番Vercelで初めて動くので、観測する。
5. 切り戻しの条件（いずれか）: 窓内取得率が98%を下回る、`expired`が出る、エラー率が有意に上昇する、429・503が出る、クォータ超過 → §7のコマンドで即時に切り戻す。

### 4.3 ジョブ別

| ジョブ | 方針 | 特記事項（レビューの追加条件を含む） |
|---|---|---|
| 結果取得（`result`・`result_catchup`） | shadow 1日（9/20夜〜9/21で24/24一致、`expired` 0）→ 日中に、対話中に、live化とGitHub側停止 | 前提: G6のSlack到達の確認。今夜の予測リフレッシュ案1の有効化（23:10）と、同じ観測窓にしない（G8。案1の有効化の約15時間後の、9/22午後）→ **9/22午後**。切り替え後、最初の実書き込みと、`result_catchup`の00:30を観測。**的中フラグ補完（`fixMissingHitFlags`）は、従来は結果取得のたび、移行後は日次（23:50・00:30）になり、欠落の自己修復が最大1日遅れる**（既知の挙動差） |
| Kファイル同期 | `?probeDate=`でLZH展開を確認 → **直接live（GitHubと並走、同値は書かない）1日** → GitHub側停止（shadowは、対象日が`nothing_pending`になりやすく、ほぼ空振りのため意味が薄い） | 復旧可能（4日ルックバック）。**2026-09-21 15:06 JSTにprobe成功（2026-09-19、156レース）し、live並走を開始**（最初の実書き込みは9/22 07:00） |
| **オッズ** | **前提: 予測リフレッシュ案1の有効化（今夜23:10）と、runbook J-2の確認**（案1が先。オッズをVercelへ移すと、GitHub側の再計算のきっかけが消えるため）→ **shadowを省き、live並走1〜2日**（`source`で区別。runbook M-3のSQLで、窓別の窓内取得率をsource別に比較）→ 早朝（06:30〜07:00）に、**人間のgo**のもとで、GitHub側を停止し、親が最初の窓を観測 | 0分窓は、補完（直近スナップショットからの`window_min=0`の補完）が、shadow中は`source='gha'`の行、GitHub停止後はVercel自身の-5・-10分の行になり、条件が変わる。**0分窓の`outcome`内訳（補完で完了した件数）を、切り替え後の毎日の確認項目にする**。Aの起点（時系列オッズは取得開始日以降のみ）は、**2026-09-21にユーザーが承認済み**（全券種オッズは、2026-09-17以降のみを期待件数の対象とし、それ以前は「取り直せない」として除外する。`data-health-report.js`の分母にも反映する） |
| 日次ジョブ | shadowを省き、GitHubと並走の`live`で1周回して、GitHub側を停止（上書き型で、同値は書かない） | 選手プロフィール（月次）は、手動のdry-run → 少数 → 全件。fan取り込みの実施後に、B6を縮小 |
| レース情報（A1）・展示（A2） | **コード完了（2026-09-21）**。shadow 1日 → live。A2は展示スロット化で窓が3→1に変わるため、shadowでは挙動差を測れない。WS2の取得時刻で、旧・新の7分前までの取得率・発走前の分数を比べる。展示は、`scrape_job_state`の行が無い間は従来の経路がそのまま動く（マージで止まらない）。手順・SQL・切り戻しは[verification-runbook.md](./verification-runbook.md) Q | 前提: 案1（`REFRESH_ON_VERCEL`）の確認（liveの前提。shadowは不要）。**展示（A2）は2026-09-21 13:34 JST、レース情報（A1）は同日約14:20 JSTにshadowを開始**（書き込みなし。日中の開始のため、起動直後に未実行の警告が1回きり出る）。live化は、展示→レース情報の順、別の日（同じデータを扱うため。G8）。A1はracelistのみ取得（beforeinfoの重複D2を解消。気象は展示の値） |
| `races-init`（A0） | **コード完了・shadow中（2026-09-21 12:58 JST〜）**。**人間のgoを残す**（全窓型ジョブの前提のため）。GitHubの07:00初期化が残る間は自己修復する | 所要時間の実測（U12）が前提 |
| pcexpect（A8） | 復旧可能 → 短縮可 | 所要時間の実測が前提 |

## 5. リスクと緩和

| リスク | 緩和 |
|---|---|
| オッズの取りこぼしは永久損失 | live並走1〜2日、早朝の切り替えと親の観測、`expired`の即時通知、フェイルセーフ付きSKIP（§6）、1コマンドの切り戻し（§7） |
| 全ジョブとmonitorが同じVercelに載る（共通原因障害）。GitHub側のメタ監視は日次（09:30 JST、GitHubの遅延は2〜5時間） | フェイルセーフ付きSKIP（§6）。切り替え完了までの間、死活確認を毎時にする（実装は、別タスクとして検討） |
| shadow中のmonitorが、`expired`・窓内取得率を集計しない | G6・G2を、`check-*-shadow.js`での確認に変更。monitorの拡張は、別タスク |
| 429・503がDBに残らない | G4を、ブレーカーとVercelログの目視に変更。`last_report`への件数記録は、別タスク |
| 取得先への負荷（shadow中の二重取得。オッズ約5,000ページ/日、結果約700ページ/日で、現行の約12〜13千/日に対して約4割増の推定） | オッズはshadowを省く（live並走で置き換え）。ホスト単位のブレーカー（`host:boatrace.jp`は全Vercelジョブで共有）。切り替え後は、総負荷が減る見込み（約8.0千/日） |
| Vercelの使用量・費用 | 9/14〜20の実測: 合計約21.1ドル、うちビルドCPU 16.3ドル（BOA-361の再デプロイ由来）、関数関連は約1.0ドル（result shadow開始前）。live化後の増分は、切り替え後に実測する |

## 6. GitHub側の停止のフェイルセーフ（自動フェイルオーバー。レビューの代替案C）

静的なSKIP変数だけだと、Vercelが不健全になったとき、人間が変数を戻すまで、取得が止まる（無人の夜間は、特に危険）。`scrape-scheduled.js`のスキップ判定を、次のようにする（別タスクで実装。**実装が終わるまで、GitHub側の停止は行わない**）:

- `SKIP_<JOB>_ON_GHA=true`のとき、GitHub側は、`scrape_job_state`を読み、**`mode='live'`かつ`last_success_at`が、そのジョブの想定間隔の数倍以内**なら、スキップする。
- それ以外（`mode`がlive以外、成功が古い、**DBの読み取りに失敗した場合を含む**）は、従来どおり実行する（フェイルセーフ）。
- 効果: 通常時は、二重取得なし。Vercelが止まったときは、GitHub側が自動で肩代わりする。人間の操作なしで、復旧する。

## 7. 切り戻し（1コマンド）

```bash
# 1. GitHub側を再開（次の実行から即時）
gh variable set SKIP_<JOB>_ON_GHA --body false -R rhapsody0919/boatrace-ai-predictor
# 2. Vercel側を止める（DBの更新。再デプロイ不要）
#    UPDATE scrape_job_state SET mode='off', updated_at=now() WHERE job='<ジョブ>';
```

切り戻し後も、Vercelが`live`で書いた行は、そのまま残る（上書き型でGitHub側と同形。オッズは`source='vercel'`の行が残る）。結果取得とオッズは、変数とmodeが別で、独立に戻せる。共通原因障害（Vercel本番の不具合）は、独立に戻せない（§6のフェイルセーフで対処）。

## 8. 実測（レビュー時点、2026-09-21）

**旧基盤（GitHub Actions、`source='gha'`）のオッズ窓内取得率**（9/14〜20、会場開催レース1,164件。runbook M-3のSQL）

| 窓 | 7日 | 9/17〜20 | 9/19〜20（週末） |
|---|---|---|---|
| 60分前 | 89.6% | 88.7% | 93.2% |
| 30分前 | 92.4% | 91.4% | 96.6% |
| 15分前 | 93.0% | 92.5% | 97.5% |
| 10分前 | 93.0% | 92.3% | 97.2% |
| 5分前 | 93.5% | 93.3% | 97.5% |
| 0分前 | 64.9% | 92.0% | 97.2% |

（0分窓は、保存の開始が9/16のため、7日値が低い。ドラフトの「0分前52.8%」は、別の期間の値で、旧基盤を軽く見る根拠にならない）

- **結果取得のshadow**（10:55 JST時点）: 24件が`done`で一致24/24。遅延のp95=70.3分は、shadow開始時（21:18）に9件をまとめて消化した分を含む（実際の遅延は5〜20分）。
- **Vercel Cronの配信**: 直近3時間で、`/api/cron/result`180回、`/api/cron/odds`180回、`scrape-monitor`36回。欠落なし。
- **オッズスロットの負荷**: 9/20（168レース）で、1分あたりの期限到来スロットは最大6件、平均1.7件（`claimLimit=24`に余裕）。
- **暦**: 9/21（月）は敬老の日、9/22（火）は国民の休日（推定）、9/23（水）は秋分の日。「平日サンプル」は9/24以降。

## 9. 独立レビューと対応（2026-09-21）

独立したエージェントが、実コード・本番の実データ（読み取りのみ）・文書で検証した。結論は「**修正のうえ採用。オッズは原案のまま採用すべきでない**」。

| # | 重要度 | 指摘 | 対応 |
|---|---|---|---|
| 1 | 致命 | オッズのshadowは弱い（ダイジェストは構造のみ、書き込み経路・値の正しさ・0分窓の補完が検証されない） | **採用。** オッズはshadowを省き、live並走1〜2日に置き換える（取得負荷はshadowと同じで、増えない）。ユーザーの「並走は不要」という意見のうち、負荷への懸念は、この置き換えで解消する |
| 2 | 致命 | 0分窓の補完の条件が、切り替え後に変わる | **採用。** 0分窓の`outcome`内訳を、切り替え後の毎日の確認項目に |
| 3 | 重要 | 「live並走で分かるのは書き込みだけ」は誤り | **採用。** §3で訂正（追加で分かる5点） |
| 4 | 重要 | 「live並走の代価は大きい」は、オッズでは過大（shadowも同量を取得） | **採用。** §3で書き直し |
| 5 | 重要 | 「旧基盤0分前52.8%」は再現しない | **採用。** §8を実測に差し替え。G5を「98%以上」に |
| 6 | 重要 | monitorはshadowの`expired`・窓内取得率を集計しない。Slackは一度も実弾で通っていない | **採用。** G2・G6を変更。切り替え前のSlack到達確認を必須に |
| 7 | 重要 | pendingへ戻すSQLに期限条件が無い | **採用。** §4.2の期限つきSQLに |
| 8 | 重要 | 「夜間に切り替える」と「対話中のみ」が矛盾 | **採用。** G7を変更（日中、対話中。オッズは早朝、人間のgo） |
| 9 | 重要 | オッズの前提（案1の有効化とJ-2の確認）が欠けている | **採用。** §4.3のオッズ行に前提として明記 |
| 10 | 重要 | 429・503はDBから測れない | **採用。** G4を、ブレーカーとVercelログの目視に |
| 11 | 重要 | 全ジョブとmonitorが同じVercel（共通原因障害）。メタ監視は日次 | **採用。** §6のフェイルセーフ付きSKIP（自動フェイルオーバー）を追加。実装が終わるまでGitHub側を停止しない |
| 12 | 重要 | 結果取得は約170スロット/日で、99%は統計的に証明できない | **採用。** 復旧可能であることを理由に許容と明記 |
| 13〜14 | 軽微 | 所要時間はVercelログのみ／的中フラグ補完が日次になる | **採用。** G3・§4.3に記載 |
| 15 | 軽微 | 9/21〜23は休日で、平日サンプルは9/24以降 | **採用。** §8・日程 |
| 16 | 軽微 | shadowの遅延p95に、開始時の一括分が含まれる | **採用。** §8に記載 |
| 権限 | — | 親の権限は、復旧可能なジョブのみ。オッズとraces-init/morning-initは、人間のgoを残す | **採用。** §10 |
| 代替案 | — | A（オッズはlive並走1〜2日）+ C（自動フェイルオーバー）が費用対効果最大 | **採用。** §2・§6 |

**反論した点**: なし。ただし、ユーザーの意見（オッズの並走は不要）に対しては、「負荷の懸念は、shadowをlive並走に置き換えることで解消する。一方、書き込み経路・値・0分窓の補完を、shadowでは検証できないため、live並走は、最低1日残す」と回答する。

## 10. 親（Claude）の切り替えの権限（ユーザー承認、2026-09-21。改訂版）

- **親が行ってよい**（§4.1の基準を満たし、対話中）: 復旧可能なジョブ（結果取得・result_catchup、Kファイル同期、日次ジョブ、pcexpect）の、`scrape_job_state.mode`の更新と、`SKIP_<JOB>_ON_GHA`の変更。切り替え後の観測と、基準を外れた場合の切り戻し。
- **人間のgoを残す**: **オッズ**（取り直せない）と、**`races-init`/morning-init**（全窓型ジョブの前提）の、切り替えの瞬間。
- **行わない**: 基準が満たされない切り替え、429・503・取得先の警告があった場合の継続（止めて報告）、無人の夜間の切り替え、DDLの適用、データの修正、外部サービスの設定変更（承認済み以外）、ジョブの新規追加、§6の実装が終わる前のGitHub側の停止。
- **必ず報告する**: 切り替えのたびに、根拠の数値（基準の実測）と、切り戻しの手順。
- **実行上の制約（2026-09-21判明）**: Claude Codeの自動判定（auto mode）は、本番の`scrape_job_state`のlive化を、ユーザーが対象のジョブ名と操作を明示した指示（例:「`point_rank`をliveにして」、SQLの貼り付け）が無いと拒否する。shadowの開始は通る。live化は、事前にユーザーの明示の指示をもらってから実行する（§10の権限は、判断の権限で、実行の許可とは別）。

## 11. 日程（再前倒し後の見込み。2026-09-21改訂・3回目、G8撤廃。推定）

2026-09-21夕方、ユーザーが、切り替えの1日の件数の上限（G8）の撤廃を承認した（なるべく早く完了させたいため）。件数の上限がなくなったので、各ジョブの切り替えは、それぞれの前提（shadowの標本・案1の確認・ユーザーのgo）が揃った時点で行う。

| 日 | 内容 |
|---|---|
| 9/21（今日） | shadow: 結果取得・pcexpect・`races_init`・展示（A2）・レース情報（A1）。live並走: 得点率（22:00が最初の実書き込み）・進入コース別（20:00）・Kファイル同期（9/22 07:00）。**オッズのlive並走を、ユーザーの指示があれば今日から開始**（なければ9/22朝）。今夜23:10に予測リフレッシュ案1の有効化。Slack到達は、13:50の警告の受信で確認済み |
| 9/22（国民の休日） | 朝に案1の確認（runbook J）。`races_init`のshadow初回の全会場の標本（05:00起動、07:35以降に比較）。**午後に一括でlive化**: 結果取得・pcexpect・展示（A2。13:45以降、24時間分のshadowの標本の判定後）・レース情報（A1。展示の最初のliveの窓が完了してから。案1の確認後）・選手ニュース・会場別モーター成績（live並走）。**`races_init`のlive化は、昼〜夜にユーザーのgo**（最初の実書き込みは9/23 05:00）。オッズのlive並走を、9/22朝に開始する場合はここ |
| 9/23（秋分の日） | 05:00に`races_init`の最初の実書き込みを観測。**06:30にオッズのGitHub側停止（ユーザーのgo）**。**全ジョブの切り替えが完了する見込み**。展示のcron-job.org停止は、liveで1日以上安定した後（9/24以降。ユーザー作業） |
| 9/23〜9/27 | 切り替え後5日の実測（土日を含む） |
| 9/28〜 | 完了の定義A・B・Cの最終確認、cron-job.orgと取得系GitHub Actionsの停止（WS7）。**Aは、K/Bの取得（下記）に律速される** |

見込み: **切り替えの完了は9/23、旧基盤の停止は9/30〜10/1頃**（初回の前倒しの見込み9/26・10/3頃より、約3日早い）。**完了の定義の達成は、K/Bの取得の完了に律速される**: 残り約5,456リクエスト（残り4,300 + 欠損月の約580 + fan・monthlyschedule）を、夜間のみ（22〜06 JST）で取ると、1夜約900リクエストで、9/26〜27まで掛かり、切り替えの完了を早めても、完了は9/28〜29頃から変わらない。**取得の窓を終日にすると約2日で終わり、Aの完了が9/25〜26に縮む**（取得先はファイル配布サーバーwww1.mbrace.or.jpで、間隔3〜5秒は同じ。ユーザーの判断事項）。前提: 各切り替えが、1回で基準を満たす。基準を満たさない場合は、原因を直して、やり直す（1〜2日の遅れ）。

**前倒しで変わらない制約**: 案1の有効化（今夜23:10）と確認（9/22朝）は省けない（オッズのGitHub側停止・レース情報のliveの前提）。オッズは取り直せないため、live並走を最低1日行ってから停止する。`races_init` とオッズの停止は、ユーザーのgoを残す。

## 12. 第1弾の完了の範囲（2026-09-21承認、独立レビュー後）

取得の完了は2段階。**第1弾（9/29）**: 現行の取得のVercel一本化、実装済みの拡張項目（結果の全項目・出走表・直前情報の全項目・fan・monthlyschedule・選手コメント・特記事項）、K/Bの過去分（2019-04〜）の取り込みと本体への補填、および次の編入項目。**第2弾**: 残りの未取得項目（第1弾の期限までに間に合わなかった項目を含む）。

| 項目 | 第1弾での扱い | 完了の定義Aの例外・注意 |
|---|---|---|
| N23 前検・節のモーター2連対率 | 日次のスナップショット（朝の07:00前か夜）+ 2025-12〜のバックフィルCLI（約830リクエスト） | 期待件数は`race_entries`から算出 |
| N25・N26 BOATCAST | **日次取得のみ**。公開マップを事前凍結。403はジョブ内で扱う（共有の`politeFetch`は変更しない） | **過去分（約44,700）は第1弾の対象外（例外）**。江戸川は常時403の見込みで対象外。未確認会場は別扱い |
| N29 日次照合 | 既存の`kfile_sync`のKファイルとDBの突合（新規の取得先なし） | 照合不能（Kが未取得の日）は別に数える |
| N19 3連率 | 導出をやめ、racelistの直接取得（約8,692リクエスト、夜間・逐次）に切り替える案。**ユーザーの承認後に実行** | 導出値を`race_entries`に混ぜない |
| N11 過去日の出走表・直前情報 | **見送り**（効果測定後に再判断） | **`today_weight`・`tilt`等の過去分の充足は、Aの例外** |
| 完了の定義C（汎用の日次監視） | **必須**。期待件数SQLを宣言的に登録し、閾値未達・空テーブルをSlackへ。監視自体の失敗も検知 | 独立レビューで未実装と判明 |
| K/B取り込み後の補填 | **必須**。結果欠損（2025-12・2026-01・2026-03）の本体への昇格、`race_grade`のNULL（2025-12〜2026-02）の補完 | K/Bは別テーブル`kb_archive_*`に入る設計 |

**締め切り**: 新規テーブルにだけ書くジョブ（N23・N25・N26・N29）は、shadowを省き、直接liveにできる（既存データに影響しない。切り戻しは`mode=off`）。判断は9/23、9/24 24:00に閾値（A・B）を満たさない項目は第2弾に落とす。**live化は、ユーザーが対象ジョブ名を明示した指示が要る**（§10）。

**項目別のB基準**: 各子が、tasks.mdに「提案」として書く。ユーザーが承認するまで、その項目のBは確定しない。
