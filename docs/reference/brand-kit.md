# ブランドキット

龍神レーダー（boat-ai.jp）のnote/X/YouTube/TikTok/ブログ/静的ページ向けビジュアル制作で参照する単一ソース。トークン集ではなく、**承認済み実例のギャラリー**として運用する（詳細: [`docs/design/content-ops-flow/spec.md`](../design/content-ops-flow/spec.md) C1）。

**新しい画像・動画を作る前に必ずこのファイルのギャラリーを確認すること。** ゼロから発想せず、既存の採用実例を出発点にする。承認を得たら、その場でこのファイルに実例を追記すること（後日まとめて更新しない）。

---

## トークン

実際のサイトヘッダー（[`src/components/Header.jsx`](../../src/components/Header.jsx)・[`src/styles/design-tokens.css`](../../src/styles/design-tokens.css)）の実値をここに転記する。ここはミラーであり、実値の変更は必ずソース側で行う。

### ロゴマーク
`/logo-light.png`（1320×1405px、透過PNG）。龍の紋章＋波＋帆（レーダーの三角形）を円形に配置したデザイン。ライト/ダーク両テーマで同一ファイルを使用（透過のため両背景で機能する）。**新しいロゴバッジ・紋章を独自に作らない。既存のこのファイルをそのまま使う。**

### カラー
| トークン | 値 | 用途 |
|---|---|---|
| `--ryujin-gold-500` | `#c9a227` | ゴールド本体色（ダーク背景用） |
| `--ryujin-gold-800` | `#6b500f` | ゴールド（ライト背景でのコントラスト確保用） |
| `--ryujin-silver-500` | `#9aa3ad` | シルバー本体色（ダーク背景用） |
| `--ryujin-silver-700` | `#5c636b` | シルバー（ライト背景用） |

### タイポグラフィ
ロゴテキスト「龍神レーダー」は**分割ウェイト・分割色にしない**。単一ウェイト（`font-weight: 700`）・単一色（ゴールド）で表示する（2026-08-21のADR 0018で明朝体から統一済み）。フォントスタックは `-apple-system, BlinkMacSystemFont, "Segoe UI", "Noto Sans JP", "Hiragino Sans", sans-serif`。

### タグライン
「ボートレースを見える化。迷ったら、データを信じる。」（`common.json`の`nav.logoTagline`と同一文言をチャネル素材でも使う）

---

## チャネル別ギャラリー

### note ヘッダー画像（1920×1006px）

- **採用（2026-09-01）**: header-e — 実際の`/logo-light.png`を1650pxまで拡大しキャンバス右端でブリード配置（枠からはみ出す大胆な配置）、頭部付近だけ鮮明な小サイズを重ねて奥行きを出した。テキストは単一ウェイト・ゴールド一色で実ヘッダーと統一
  - 却下理由の記録: 初回に「空白が多い」という指摘を受け、ロゴを主役として大胆に使う方向へ作り直した結果採用に至った
- **却下**: header-D — 実ロゴ・実フォントは正しかったが、ロゴを110px程度の小サイズで左上に配置しただけで右側に大きな空白が残った（「もっと天才的な使い方ができないか」との指摘で却下）
- **却下**: header-C2 — 独自の六角形バッジ＋「龍」一文字の紋章、ロゴテキストも「龍神」太字ゴールド／「レーダー」細字白の分割ウェイトで構成。**実際のサイトヘッダーと異なるロゴ・フォントを新規に作ってしまった**ため却下（「ベースはトップページのヘッダーと同じデザインイメージ。同じフォントや同じロゴを使うこと」「龍紋章は不要」との指摘）
- **却下**: 初回案（絵文字🐉ベース） — 環境依存でカラフルなイラストとして表示され浮く。「クオリティが低い、天才デザイナーにゼロから作成させて」との強い指摘で作り直し

### note アイコン（正方形）

- **採用（2026-09-01）**: icon-a — 実際の`/logo-light.png`をそのまま紺の放射グラデーション背景に中央配置。ヘッダーと同じ「独自バッジを作らず実ロゴをそのまま使う」方針
- **却下**: 初回案 — 絵文字🐉ベースの独自デザイン。ヘッダーと同じ理由で却下

### note 埋め込み動画のCTAシーン

- **修正・反映済み（2026-09-01）**: `sns-video-studio/remotion/src/noteVideoShared.jsx`の`Logo`コンポーネントが、noteヘッダーと矛盾する絵文字🐉ベースのバッジを使っていたことが判明（同じnoteチャネル内での不統一）。実際のサイトロゴ画像に差し替え、公開済み3本（データ出走表・回収率分析・好調不調ランキング）を再レンダリング→YouTube再アップロード→note記事3件の埋め込み更新まで完了

### X / TikTok 動画CTA（2026-09-01、ソースコード監査で判明・共通コンポーネント新設済み）

- **採用**: [`sns-video-studio/remotion/src/snsVideoShared.jsx`](../../sns-video-studio/remotion/src/snsVideoShared.jsx)（新設）の`SceneCTA({ ctaLines, subLine })`。実際のサイトロゴ（`noteVideoShared.jsx`の`Logo`を再利用、`/logo-light.png`）＋確定CTA文言型を組み込んだ共通コンポーネント。**新規のX/TikTok動画テンプレートは必ずこれを使う**
- **却下の経緯（ロゴ）**: 監査時点で21本の動画テンプレート（`sns-video-studio/remotion/src/*CM.jsx`）は全てCTAシーンが独自実装で、テキストのみ「boat-ai.jp」と表示するか、絵文字🐉ベースの独自バッジ（noteヘッダー制作時に却下したのと同じパターン）を使っていた。ロゴ画像は一切使われていなかった
- **CTA文言の基本型**: 「[話題]、無料で見れる/使える」を公式ルールとして確定（2026-09-01）。実例: 「全24会場のデータ、無料で見れる」「回収率データ、無料で見れる」。マンネリ防止のための話題部分のバリエーションは許容する（`docs/operation/x-operations-playbook.md`のマンネリ対策と整合）。**この型から外れる表現（絵文字付き「🎯これ、全部無料で使える」、「登録不要・アプリ不要」の別文言併記等）は今後の新規制作では使わない**
- **URL表記**: 「boat-ai.jp」（小文字）で統一済み、問題なし
- **既存21テンプレートの移行**: 未着手。`content-quality`ラベルで[BOA-232](https://linear.app/boat-ai/issue/BOA-232)にLinear起票済み。各テンプレートを個別に新コンポーネントへ移行し、都度実際にレンダリングして視覚確認する必要があるため一括の機械的置換はしない

### アプリ内公式文言の使い分け（2026-09-01確定）

サイト内には役割の異なる2つの公式文言が存在する。どちらも正しい使い分けであり、統一は不要:

- **`nav.logoTagline`**（ヘッダーロゴ下・フッター・チャネル素材共通）: 「ボートレースを見える化。迷ったら、データを信じる。」— ブランドの詩的なタグライン。note/X/TikTok等のチャネル素材はこちらを使う
- **`<title>`タグ・OGP description**（`useSocialMeta.js`・`common.json`の`meta.title`）: 「龍神レーダー - 無料のボートレースAI予想＆データ分析」— SEO・検索結果表示向けのキーワード重視の説明文。**変更しない**（検索順位・CTRへの影響が読めないため、変更するなら別途SEO施策として慎重に検討する）
- 2026-09-01時点で`introBannerText`・`footerText`は「無料のボートレースAI予想＆データ分析」から`logoTagline`に統一済み（PR参照）。`<title>`・OGP descriptionは意図的に未変更

### YouTube / ブログ

未整備。承認された実例が出るたびにここへ追記する。ブログのカバー画像は実機能のスクリーンショットのみで構成し、ロゴ・CTAは意図的に含めない方針（既存の「装飾目的の画像は不要」ルールと整合、`.claude/CLAUDE.md`フローA-3参照）。

---

## 運用ルール

1. **新規制作の着手前に本ファイルのギャラリーを確認する**（[`docs/design/content-ops-flow/spec.md`](../design/content-ops-flow/spec.md) C2）。既存の採用実例と矛盾する独自デザイン（新しいロゴバッジ、実ヘッダーと異なるフォント処理等）を作らない
2. **承認されたら、その場でギャラリーに追記する**（C3）。追記せずに次のタスクへ進まない
3. **却下された案も理由付きで残す**。同じ失敗（独自紋章の再発明、環境依存の絵文字使用等）を将来のセッションが繰り返さないようにする
